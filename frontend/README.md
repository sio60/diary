## frontend install
npm install

# 네비게이션 + 보안 저장소
npm i @react-navigation/native @react-navigation/native-stack
npx expo install react-native-screens react-native-safe-area-context expo-secure-store
npx expo install expo-splash-screen expo-font
# URL/Fetch 폴리필(네이티브 환경 호환 보강)
npm i react-native-url-polyfill

# start
npx expo start
