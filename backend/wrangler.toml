name = "exchange-diary-api"
main = "src/index.js"
compatibility_date = "2025-11-12"
[vars]
# 배포 환경에서는 wrangler secret 로 넣는걸 권장
# CORS_ORIGIN 예: 앱 프론트 배포 도메인
CORS_ORIGIN = "https://diary.codedrive25.workers.dev"

[placement]
mode = "smart"

[observability]
enabled = true
